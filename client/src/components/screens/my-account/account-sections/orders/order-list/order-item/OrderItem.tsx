import { IOrderItem } from '@/shared/interfaces/order.interface'
import { FC } from 'react'

const OrderItem: FC<IOrderItem> = ({ quantity, product }) => {
	return <div>OrderItem</div>
}

export default OrderItem
